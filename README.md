This is a Next.js starter template. 

It's a create-next-app generated application using the default styling but with a Layout, Navigation component and Grid added. 

The Cards in the Grid are dragging in some data from an external API. 